export enum STATUS_QUERY {
  INITIALIZE = 'initialize',
  LOADING = 'loading',
  DONE = 'done',
  ERROR = 'error',
}

export const DATE_FORMAT = 'YYYY-MM-DD'

export const THEMES = {
  DARK_THEME: 'dark',
  LIGHT_THEME: '',
}
